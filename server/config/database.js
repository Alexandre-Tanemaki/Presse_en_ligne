require('dotenv').config();

module.exports = {
    host: 'localhost',
    user: 'root',
    password: '',
    database: 'ma_presse',
    waitForConnections: true,
    connectionLimit: 10,
    queueLimit: 0
}; 